<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quizzapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Quiz.Geography"></activity>
        <activity android:name=".Quiz.Politics" />
        <activity android:name=".Quiz.Sports" />
        <activity android:name=".Quiz.Technology" />
        <activity android:name=".Quiz.History" />
        <activity android:name=".Quiz.GeneralKnowledge" />
        <activity android:name=".Quiz.ArtAndLiterature" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashSreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>